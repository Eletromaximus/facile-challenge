generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("PSQL_URL")
}

model database {
  id     Int    @id @default(autoincrement())
  hash   String
  buffer String
}
